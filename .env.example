# ===== Fechamento de Caixa - Environment Configuration =====

# Vercel Deployment (CI/CD)
VERCEL_PROJECT_ID=seu_project_id
VERCEL_TOKEN=seu_token_ou_api_key

# MongoDB Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database_name>?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-key-that-is-at-least-256-bits-long
JWT_EXPIRATION_MS=86400000 # 24 hours

# Server Configuration
SERVER_PORT=8081

# ===== SECURITY NOTICE =====
# 1. Copie este arquivo para .env para desenvolvimento local.
# 2. Substitua todos os valores de placeholder (como <username>) pelos seus reais.
# 3. O arquivo .env está no .gitignore e NUNCA deve ser versionado.
# 4. Mantenha todos os segredos seguros e rotacione-os periodicamente.
